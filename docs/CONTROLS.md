# URL hash controls (web-three)

Every hash param the runtime actually parses (`applyHashParams()` plus the
early `#q=` read in `web-three/js/main.js`). Hash, not query — no server
round-trip, so the deployed sim stays a pure static file. Combine with `&`:

```
web-three/#preset=secondpeak&cam=cliff&tide=-0.5&sim=42&controls=0
```

## The hash round-trips (2026-08-16)

Params of kind **control** are now written back to the URL as you use the app,
so the address bar is always a shareable permalink of what is on screen. Params
of every other kind (`instrument`, `A/B revert`, `feature flag`, `sweep knob`,
`debug`, `compatibility`) are **boot-only**: read once at load, never written.

Three rules follow, and they are the whole contract:

1. **Only what you chose is written.** A default-state view serialises to a bare
   URL. `surfer=0`, `speed=1`, `bed=reef`, `preset=secondpeak`, `cam=free` are
   omitted rather than spelled out. The trade-off is deliberate: if a shipped
   default ever changes, an old link that omitted it changes meaning too, so an
   author pinning a specific view for a capture or an essay embed should keep
   writing the value explicitly.
2. **Boot-only params you typed are preserved.** Loading `#m4=0&sim=42` and then
   nudging the tide keeps both — the writer owns the control keys and nothing
   else.
3. **Editing the URL by hand does the right thing.** A change confined to
   control params re-applies live. A change to the boot-only set — adding one
   *or removing one* — reloads, because those are applied once at boot and the
   live path does not touch them. Removing is not the symmetric no-op it looks
   like: dropping `m4=0` from the URL without a reload would leave the app at
   `m4=0` while the URL claimed the default.

Writes are debounced (120 ms) and use `replaceState`, so dragging a slider is
one write rather than sixty and the back button is not filled with slider
frames. `replaceState` does not fire `hashchange`, so the app never re-reads its
own writes. Serialisation order is fixed and matches the table below, so the
same view always produces the same link.

Not covered: `#day=live` writes `day=live` back, but the fetched ocean is
whatever the nowcast says *at load time* — the link reproduces the mode, not
the conditions. That is the intended meaning of `live` and not a defect.

Two kinds of flag, named as such below: a **feature flag** gates work that is
landed but not yet default (default off, judged as an ensemble — TODO Track 1c);
an **A/B revert** turns a shipped default OFF so a regression can be bisected
without checking out old code. (The ensemble was judged on 2026-08-13 and
REJECTED — no combination of the four feature flags ships; see
`WEB_THREE_SPEC.md` "The ensemble, judged". They remain individually useful for
A/B measurement.) A third kind, **sweep knob**, exposes a shipped constant so
it can be measured across a range; it defaults to the shipped value.

| param | values | default | what it does | kind |
|---|---|---|---|---|
| `preset` | `sewers` `firstpeak` `secondpeak` `jacks` `thehook` `sharks` `privates` | `secondpeak` | site preset: reef, stage bounds, card ocean (params.js) | control |
| `day` | a conditions.js key (`small` `modelcard` `pulse` `overhead` `big` `stormy` …) or `live` | preset ocean | named condition day rides on top of the preset (ocean only, never the reef); `live` pulls today's MOP SC116 nowcast, cache fallback. H₀ is routed through the **peel floor**, which binds only where the day sits on the floor's measured basis (tide 0, card T) — no shipped day does, so all six pass through unchanged; see `clamp` below and MODEL.md §4.6 | control |
| `month` | `january` … `december` | none — the site card | climatological month: sets H₀ to the **p75** swell height typical of that month at CDIP SC116, de-shoaled to deep water (`data/climatology/pp_monthly_ocean.js`). **Size only** — period is seasonless here (14.4–15.2 s every month), and tide/chop/Δf are not in the bulk pull. Mutually exclusive with `day`; an explicit `h0` wins. Percentile named in the HUD. **Opt-in: a bare URL is each spot's own card ocean.** A global January default shipped briefly on 2026-08-16 and was reverted — it replaced all seven per-spot card H₀s, which are the calibration input for `shared/model-glsl.js` `SHELTER_*`, and collapsed the peel at Sewers (α 38→5) and First Peak (50→1). `card` is accepted as an alias for "no month". **The p75 is a request, not the drawn height**: it is raised to the spot's measured peel floor where it falls below one (`clamp` below), which at Sewers and First Peak flattens all twelve months onto one height and leaves the control varying nothing there. The HUD names both numbers when that happens | control |
| `drift` | `1` | off | screensaver mode: hard-switch through the surf-worthy condition days every 300 s of sim time | control |
| `tide` | metres MSL, clamped to [−0.862, 0.764] (MLLW…MHHW) | 0 | water level; moves the break position, not the breaking depth | control |
| `bed` | `reef` `plane` `measured` | `reef` | seabed A/B: measured+synthetic reef / least-squares plane / measured only — the causal demo for the peel | control |
| `surfer` | `1` / `0` | off | show the procedural rider | control |
| `section` | `1` | off | cross-section chart overlay (section.js) | control |
| `controls` | `1` / `0` | shown | explicitly show or hide the app bar, drawer, and reveal affordance; `1` overrides Tour's clean-screen default | control |
| `hud` | `1` / `0` | shown | legacy alias for `controls`; retained for existing permalinks and captures | compatibility |
| `audio` | `1` | off | arm procedural surf audio; starts on the first user gesture (browser policy) | control |
| `sim` | seconds | 0 | seed the sim clock — deterministic captures (`sim=42` is the house probe clock) | instrument |
| `speed` | 0–4 | 1 | sim time scale (`0` freezes) | control |
| `h0` | metres, clamped 0.4–3.0 | preset | override swell height | control |
| `cam` | `free` `cliff` `lineup` `cover` `drone` `point` `follow` `tour` | `free` | camera preset; `tour` is the screensaver auto-cut. **`cover`** (2026-08-22) is the close-up: it stands 16 m off the baked line's action centroid — the travelling breakpoint — down-point of it so the face is presented rather than seen edge-on, seaward of it so it looks *into* the curl, with the eye 2.4 m off the water and fov 28. It is the only preset built to judge **appearance** rather than to read the model: every other shot stands off far enough to keep a peel in frame, and none of them asks whether the surface is convincing *as water*. It follows the aim point like Cliff/Lineup/Drone, and it deliberately parks close to breaking water — the world-collision clamp (`#noclip`) is what keeps it out of the mesh. **`drone` is no longer near-nadir**: it was 6.25° off straight down (40 m offset under 365 m), where a crest has no silhouette and the wave read as a plan diagram; it now stands at **15° off nadir** (97.8 m offset, altitude held, so shot scale moves by 1/cos 15° = 3.5%). The QA sets sheet shoots every row at `cam=drone` and reports a bright-pixel fraction, which is a projection into that camera — **those numbers are not comparable across the tilt change**; rebuild the sheet rather than diffing `pix` over it | control |
| `q` | `high` `medium` `low` `potato` | auto | pins the quality tier (grid density — the app is vertex-bound) and disables auto-fallback | control |
| `m4` | `0` | on | A/B revert: `0` = authored break line instead of the emergent H₀Kₛ ≥ γh locus | A/B revert |
| `aim` | `0` | on | A/B revert: `0` = cameras aim via the authored `breakLineJS` proxy (pre-2026-08-18 rigs). On, Cliff/Lineup/Drone (and their Tour legs) frame the **baked** line's action centroid — stage stations, section gaps excluded — smoothed over ~6 s of sim time so a tide-moved bake glides the frame; grabbing the orbit hands the framing back to the reader until the next camera choice. No bake (unmapped site, A-frame, `m4=0`) falls back to the authored rigs | A/B revert |
| `matte` | `0` | on | A/B revert: `0` = disable the modeled-domain matte (water and land) | A/B revert |
| `shape` | `legacy` / `structural` | `structural` | A/B revert: `legacy` = pre-anatomy breaker shading | A/B revert |
| `noclip` | `1` | off | disable the world-collision camera clamp (x-ray debugging) | debug |
| `look` | `foam` / `full` | shipped image | renderer-only visual-fidelity probe (`u_fidelityLook`): `foam` = foam material only, `full` = foam + per-wave lifecycle hierarchy + connected face/lip; any other value keeps the shipped image. Named values make matched captures reviewable without numeric flag lore (`parseFidelityLook`, url-params.js) | instrument |
| `psi` | `0` | on | A/B revert: `0` = frozen-LAM plane wave instead of the baked Ψ shoaling phase field. Default ON since 2026-08-13 (M6p3 step 4) — rider and audio both solve in phase now | A/B revert |
| `crest` | `0` | on | A/B revert: `0` = pre-Track-5 read (no face darkening, no pocket foam floor, no fresh-foam white core at the line). Spec "Track 5, first pass" | A/B revert |
| `kelp` | `0` | on | A/B revert: `0` = pre-2026-08-18 kelp value polarity (bright sand lanes over the reef tongue). On, the canopy saturates over the reef band and the lanes take a mudstone bed albedo, so the wedge reads DARK as in the NAIP ortho. Measured at sewers/drone sim=42: upper-half luma std 11.4→6.7, break-band/upper-half ratio 1.97→2.20 (secondpeak 1.88→2.11) | A/B revert |
| `pitch` | `0` | on | A/B revert: `0` = the pre-2026-08-18 forward-pitch term, `theta -= skew·sin(theta)`, **and** its `q = 1.6 + 3.2·…` crest schedule (both revert together, so the A/B is exact). That term was an ODD map of θ and `crestShape` is EVEN in its argument, so the composed height was fore-aft symmetric for **every** skew: measured front/back max-slope ratio 1.000000, As −0.0001 across the whole reachable (s, q) plane. On, the map is the EVEN `theta -= skew·(1 − cos theta)` with `skew = clamp(excess·0.82, 0, 0.8)`, a flattened `q = 2.2 + 1.5·exp(−\|d\|/55)·(0.6 + 0.5·ξ)`, and the crest **locus** terms (`tSince`, `crestNear` → the pocket footprint) split off onto the unskewed carrier phase — the odd map's phase crawl at the crest had been dilating the pocket bell ~30%, and every fold/lip constant sat on top of that. Measured at secondpeak/drone (sims 42/48/54 medians), break line: As **+0.12 → −0.70** (R12 target −0.72), Sk 0.28 → 0.24 (0.28), biphase ψ **+24° → −71°** (R12 −69°), B 0.31 → 0.74 (0.775), front/back **0.96 → 2.3**, face chord 10.4° → 16.9° displayed. Weighted mean-square (Sk, As) error vs the R12 targets over 44 gauges 0.398 → 0.021; `0` reproduces the pre-fix build to 0.000000 over 720 gauge statistics. **Known cost:** the pocket footprint shrinks ×0.19–0.63 (the odd map had been dilating the `crestNear` window 1.6×), so the plunging tube at Sewers is smaller in the low profile view — drone and spilling sites read unchanged. Default ON — a defect fix restoring documented intent (MODEL.md §2.2), not a new look — but it changes every wave, so the `0` side is the bisect handle | A/B revert |
| `wwarea` | `0` | on | A/B revert: `0` = pre-4a′ foam (no re-breaking area boost). Default ON since 2026-08-13: whitewater scales with the broken area so tide legibility survives the foam gate; measured low/high bright-px ratio 1.80×→1.95× at L≥205 (2.17× at L≥160; physical band 1.93–4.95×). Earlier 2.5–2.7× readings were inflated by the Ψ-freeze dead zone suppressing inner foam — see spec Addendum 4 | A/B revert |
| `cg` | `0` | on | A/B revert: `0` = retired set-envelope group speed `0.5·LAM/T` (≈3.0 m/s at T=15) instead of the physical `gT/4π` (≈11.7 m/s) the 2026-08-13 unification shipped. Re-arms the 375 m set band the cadence audit measured, for A/B measurement in one build; drives `setEnv`/`setupLiftM` (GLSL) and the JS twins (`model-js` setEnv, `sound.js` voice envelope) together. The fixed-point beat period 1/dF does not move | A/B revert |
| `gap` | `0` | on | A/B revert: `0` = disable section-gap masking, so baked section gaps stop cutting the break envelope — the V returns | A/B revert |
| `head` | `0` | on | A/B revert: `0` = disable comet-head whitewater aging. Default ON: the first "`head=0` way better" verdict was judged on a drifted OrbitControls camera; the clean-load rematch (2026-08-14) went to `head=1` | A/B revert |
| `pock` | `0` | on | A/B revert: `0` = pocket footprint stops scaling with H_eff (fixed 7.5 m bell). On, the footprint scales by `clamp(H₀·shelter/1.5, 0.70, 1.50)` — unity at the 1.5 m model-card day | A/B revert |
| `slife` | `1` | off | feature flag: per-stripe along-crest lifecycle clock (hero read open item (a)). The canonical clock is `stripeAgeAt` in model-glsl — `tSince + phaseLag/ω`, the time since each water column's wave first broke at the line, a phase-lagged copy of the zipper's along-crest age — and the read is a post-threshold carve in GRID_FRAG (same discipline as the `#head` comet carve, extended inward): within-stripe freshness on a T/3 e-fold, stripe-to-stripe lag on 2.4·τ, so inner stripes gain a fresh-head→decayed-tail gradient pointing the peel, direction-locked to the comet by construction. Default OFF pending a live verdict | feature flag |
| `lip` | `1` | off | feature flag: aerated lip/curl (Track 5 "the lip lacks the field footage's aerated volume"; live report 2026-08-18 "the wave is curling not the foam"). The curl whitens on its own breaking GEOMETRY: a vertex-stage mask (`vAerLip`, GRID_VERT `choppyPos`) keyed to the fold's cusp parameter S and the applied lip throw normalized by face height — full white exactly on the thrown curtain, both faces, which also covers the documented dark fold-underside facets — plus a gentler capped pocket term for spilling character (low ξ crumbles at ~⅓ of a plunging curtain's white). H_eff-scaled (unity at the 1.5 m card day), section-gap masked, lull-dark via pocket's env². Read as a post-carve mix in GRID_FRAG, freshest at the traveling breakpoint on the `#arm` metric clock (85 m e-fold behind the head). Measured (capture_lip_ab.mjs + measure_lip_luma.py, curl-window medians at the head, pinned sims 36–54): sewers/drone lip band 183–225 → 210–232, lip−face ON +44/+86/+132 at three clocks (the fourth puts both bands inside one wide white curtain); OFF reproduces the pre-flag frame to ≤ 1 px. Sharks (ξ 0.45) lip moves ≤ +3 — spilling stays gentle. Default OFF pending a live verdict | feature flag |
| `arm` | `0` / `anchor` / `tail` | on (both) | A/B revert for the 6b dark-arm pair; `0` reverts both halves, `anchor`/`tail` keep only the named half. **anchor**: the group envelope is re-referenced to the LIVE break line (`u_setRef` = stage-median rayS of the baked line) with a set cresting there at t = 45 s (`SET_ANCHOR_S`) — the phase's old `s = 0` reference was the pre-M4 authored contour, and after the 6a cg unification the deterministic house clocks (sim 36–54) sampled a set NULL at the line: env 0.00–0.24, every env²-gated line foam term ≤ 0.055. Cadence 1/Δf untouched. **tail**: the comet decays over metres behind the traveling breakpoint (55 m e-fold via age·ω/\|dS/dx\|) instead of a 2.5 s clock, which collapsed to 8–20 m where the head crawls (the visible 40–70° arm) | A/B revert |
| `wrap` | `0` | on | A/B revert: `0` = the raw `mod()` foam clocks (bit-identical to the pre-2026-08-18 build). Every foam clock in the model is a sawtooth that snaps from T back to 0 when the next crest arrives, and that snap lands on a level set of `rayPhase` — a crest line, very nearly shore-parallel after refraction — so it drew a straight hard edge sweeping shoreward, terminating the foam instead of dissolving it. `crestClockS` ramps the clock back to zero over the last `CREST_WRAP_S` = 2.4 s (~14 m at c = Λ/T) instead of snapping, which is the crest's own foam-injection width. Measured at the lineup camera (sewers, sim 42): shipped foamM jumped 0.87→0.13 across one 0.25 m step and 91.6% of frame columns carried a ≥60-level one-pixel row jump; after, max Δ 0.099 and 1.5% of columns. The comet's line clock gets the same ramp (its wrap is a level set in x — a vertical seam): cometFoam jump 0.845 → 0.048 | A/B revert |
| `dline` | `1` / `2` | off | feature flag: density-composite break line (Topanga method) — `1` = density peaks feed anchor/continuity (falsified: worse chatter), `2` = per-station density mode is the line (kills low-H₀ flip chatter at Sharks; costs ~2° α). Measured 2026-08-13, spec "The density-composite line" | feature flag |
| `merge` | float m, clamped [0, 1.0] | `0` | sweep knob: how far below zero the break criterion's excess (`H₀Kₛ − γh`) must dip before a later positive run counts as a **second onset** rather than the same break continuing (`bed.js` `setOnsetMerge`, read back by `__pointbreak.onsetMerge()`). `0` = shipped, and measured **bit-identical** to the pre-2026-08-19 build. **MEASURED AND FALSIFIED 2026-08-19 — do not retry as a flip fix.** Built to attack the low-H₀ branch flips at their root: the flips are triggered by an *onset* appearing or vanishing, and the dips that vanish at all six mapped thresholds are **−0.002 to −0.144 m** of excess, i.e. the criterion grazing zero at 0.1–0.7% of its own scale over a bed whose elevation residual is 0.31–0.93 m. Merging those should have made the candidate count continuous in H₀. It does not: over the 0.40–3.00 ladder the flip count **rises** (Sewers 1→1→2→**4**, The Hook 4→4→5→**6**, First Peak 1→2→2→**3** at merge 0/0.05/0.12/0.24) while the largest jump barely moves (172→166, 116→108, 82→82 m) and the threshold merely slides down (Sewers 1.65→1.55, First Peak 1.30→1.20). A threshold on a continuous quantity relocates a knife-edge, it does not remove one — and this one adds a second discontinuity (the merge decision) on top of the first. Every mapped site's **card** α is unchanged to 0.1° at every value tested (36.4 / 51.4 / 35.9 / 32.9 / 37.0 / 26.3), so nothing regresses and nothing improves. Kept as a knob so the negative is reproducible in one build, the way `#dline` is. See MEASUREMENT_LESSONS 14 and TODO 1c'-d | sweep knob |
| `clamp` | `0` | on | A/B revert: `0` = a derived ocean draws at its raw requested H₀ instead of being held to the spot's measured **peel floor**. On (shipped 2026-08-20), a `#month=` — and any `#day=` / `#day=live` that lands on the floor's measured basis — is raised to the lowest H₀ at which that spot's peel exists: Sewers **1.61**, First Peak **1.26**, Second Peak **1.08**, The Hook **1.05**, Jack's **0.85**, Sharks **0.81** m. Privates has no measured bed, so no bake, no branch, no clamp. (`shared/params.js` `PEEL_FLOOR`; measured on the 0.40–3.00 m ladder at 0.01 m with `scripts/measure_branch_flip.mjs`, hysteresis-free — up- and down-sweeps bit-identical at 211 paired steps bank-wide plus 21 for Second Peak's refinement.) Below those heights the baked line abandons the oblique reef branch for a near-shore-parallel inshore one and stage-median α collapses to 1.0–9.1° against 36–50° targets; above them it reads 12.1–35.0. **The flip is not always the floor** — Second Peak's tabulated 1.02→1.03 branch flip moves α 2.6→3.7, a branch change between two closeouts, so its floor is the step where the peel actually returns, 1.07→1.08 (α 9.1→14.4). **The floor carries its basis and declines outside it:** measured at tide 0 and the site's card period, and the threshold is a surface in (H₀, T, tide). A month keeps card T and does not move the tide; a `#day=` moves all three, and applying the tide-0 number there was measured to make things WORSE — `#day=small` clamped up took Sewers α 12.8→3.9 and The Hook 10.4→5.9 — so `peelFloorH0()` returns null off-basis and days pass through untouched (MEASUREMENT_LESSONS 13). **Measured blast radius** (`scripts/audit_shipped_states.mjs`, 114 states, 108 baking): collapsed (α < 10°) **64 → 12**; months **52 → 0**; days **12 → 12 bit-identical**; **zero states regressed**. The twelve residuals are all off-basis `#day=` states and are untouched, not fixed. **Card states are untouched by construction** — an authored card H₀ is never routed through the clamp — and all seven measure identical, **0 of 7 differ, max \|Δ\| 0.000000** over H₀/T/tide/α/pinned/z/gap (Sewers 36.4, First Peak 51.4, Second Peak 35.9, Jack's 32.9, The Hook 37.0, Sharks 26.3). `#h0=` and the `-`/`+` keys are the reader's own number and pass through raw. **The cost, stated:** where the floor sits above the whole monthly climatology the seasonal signal is gone — at **Sewers and First Peak all twelve months clamp to one height, 0% of the 0.660 m seasonal range**, so `#month=` there varies nothing; Second Peak keeps 25%, The Hook 30%, Jack's 60%, Sharks 66%. Per-spot table and the reasoning in MODEL.md §4.6 "The peel floor". The HUD announces it whenever it binds, naming the drawn height and the requested one | A/B revert |
| `smooth` | `1` | off | feature flag: 90 m wave-scale break-line smoothing (kills A-frames, currently also the taxonomy) | feature flag |
| `peeldir` | `1` | off | feature flag: direction-monotonicity constraint on the break line | feature flag |
| `nose` | `1` or a float, clamped [0, 1.0] | off | feature flag: reef nose v2 — down-point taper of the uplift amplitude in stage fraction; `1` = the tuned 0.25, a float tunes it directly. Swept to the definitional bound 2026-08-13: the taper mechanism is EXHAUSTED, no fraction passes | feature flag |
| `reefamp` | float m, clamped [0.5, 12] | `3.2` | sweep knob: M5 wedge max uplift (`REEF_AMP_MAX`). Appears twice in the wedge — the lift clamp AND `bound`, where the reef ceases to exist — so it also sets how far seaward the reef reaches. Measured to SATURATE by ~5 m | sweep knob |
| `reefflank` | float m, clamped [14, 300] | `80` | sweep knob: wedge cross-strike feather half-width (`REEF_FLANK_W`). The effective lever on stage-median α — 45→80 nearly halves mean \|Δα\| (adopted 2026-08-13 with the retarget); 120+ is worse (shelf, not reef) | sweep knob |
| `curl` | `1` | off | feature flag: lip **overturn** as a bend. The shipped lip is a horizontal throw (`throwMag`) plus a downward drop (`dropMag`) on top of the Tessendorf fold; the fold is real (S > 1 does make the mesh multivalued) but the pair translates the crest band without preserving its thickness. (`dropMag` also used to subtract up to ~3·hM **at the pocket**, so the wave was flattest where it should be tallest — that was an independent second cause of the same read, present at default settings, and it is fixed on the default path as of 2026-08-18; see `#drop`. `#curl` masked it only because it switches the throw/drop pair off entirely.) `1` replaces that pair with the classical **bend**: water above `0.35·h_crest` curves onto an arc of radius `R = h_crest/mix(0.30, 2.60, plunge)` — `dz = dy·(1−cos θ)/θ`, `y = y_bend + dy·sin θ/θ` with `θ = dy/R`. It never lifts (`sin θ/θ ≤ 1`), preserves arc length (so the curl has thickness), and overhangs wherever the face is steeper than `1/sin θ`. ξ sets how far over it goes and how compact it is (σ_z scales with `h_crest`), the breaking excess `H₀Kₛ/γh` scales it, and `pocket` keeps it attached to the travelling breakpoint. Also caps the choppy cusp at `S = 1` so two mechanisms do not fold the same band. Exposes `vCurl` (turns of overturn), which is now the `#lip` aeration **curtain key whenever `#curl` is on** (wired 2026-08-18). Before that the two flags contradicted each other: the curtain keyed off `throwMag`, which `#curl` computes and then never applies, so `#curl=1&lip=1` painted an aerated curtain across water with no lip in it — measured at 18 of 288 Sewers stations with full white at < 0.10 turns of overturn, all now suppressed, with no strong-bend station losing aeration. Measured at Sewers q=high: max bend 132°, stray trough folds 20.4 → 14.2 m (the median-pocket-crest figure it used to quote, 5.16 → 7.88 m, measured the `dropMag` defect rather than the bend, and the default now stands at 7.96 m without `#curl`); Sharks (ξ 0.45) moves 12°, i.e. spilling barely overturns. **`h_crest` is `crestCeilM`, so on a bed-less site it is `1.878·H₀`, not a depth limit** (2026-08-19): unlike `#drop` this one is NOT inert at Privates — turning the flag on there moves 133 station fields — so the bend radius and σ_z are keyed to a reference no measurement can validate at that site. Not fixed: Privates has no measured bed to key them to instead, and inventing one is repair-the-output. See MEASUREMENT_LESSONS 12 and the TODO note. Default OFF pending a live verdict | feature flag |
| `curtain` | `1` | off | feature flag: the falling **curtain** joining the bent lip back down to the face — the geometry the 2026-08-22 overhang measurement said was missing (21 of 279 default-path overhang bins carry foam over bare water, worst gap 7.67 m). A strip mesh, shader-authored like the spray but connected: both edges are evaluations of the shipped `surfacePos` — the tip at the crest source point (so it hangs from the drawn lip), the landing `0.9·h_crest` shoreward (so it sits on the drawn face; C0 at both ends, no seam). Between them a quadratic Bézier continuing the bend's own tangent at the tip. Gated on `surfacePos`'s `curl` out — the turns of overturn the bend actually performed — times `breakMask`, so **it draws nothing without `#curl=1`** (the gate is identically zero when the bend is off); the arm to judge is `#curl=1&lip=1&curtain=1`. Measured 2026-08-24 (sewers, sim 39, x=−20, curl 0.73 = the 132° backstop): cliff-cam A/B diff is 848 px, all inside the pocket bbox; the undercut band below the detached shelf fills with streaked white. Sharks (ξ 0.45): **0 diff pixels** — spilling hangs nothing, same knob as the bend. Default OFF pending a live verdict; the down-line (inside-the-tube) read is only partially closed and is the open iteration | feature flag |
| `drop` | `legacy` | fixed | A/B revert: `legacy` restores the pre-2026-08-18 `dropMag` (GRID_VERT `choppyPos`), which flattened the wave exactly where it breaks. The term exists to bend the thrown lip down — `throwMag` translates the crest band shoreward at constant height, so the throw alone leaves a flat shelf (that is what it was added for, 3e28d38) — but it was written as `3.0·pocket·plunge·hM·lipJit`, and `hM = h/VIS` **is the height it is subtracted from**. So it was a multiplicative shrink of the whole water column wherever `pocket > 0` (up to ~0.94·h), biting hardest at the crest, which is the tallest point on it. The shipped fix keys it to `frontPhase` (zero at the crest, θ = 0; peaks ~0.9 rad shoreward — the water that has already gone over) and scopes it to the band above the same `0.35·h_crest` bend line `#curl` uses, as a fraction of that band clamped below 1 so it can never invert the crest. Measured (`scripts/measure_pocket_crest.mjs`, Sewers q=high, 4 pinned clocks × 36 stations, crest read back off the GPU): median crest / depth-limited ceiling `γh` at pocket stations **0.78 → 1.08**, against 1.05 for the same stage away from the pocket (so ~1.05, not 1.0, is the calibrated norm — `crestCeilM` is a reference height, not a clamp); monotone collapse with `pocket` (1.015 / 0.772 / 0.467 by bin) **removed**. Non-pocket stations and Sharks (ξ 0.45, `plunge` = 0, so `dropMag` was always 0 there) are unchanged to 3 decimals. **Caveat, 2026-08-19: the bend line is only depth-limited where there is a measured bed.** `crestCeilM` reads `modelDepthM`, and `bedElevM` is not gated by `u_depthMix` — on the synthetic stage it samples the 1×1 stand-in texture and returns the RGBA8 window floor, so the depth is a flat 30.9 m, `γh` never binds and `0.35·crestCeilM` is `0.657·H₀` rather than a fraction of the breaking limit. It reaches no shipped pixel today: Privates is the only bed-less preset and its ξ = 0.35 puts `plunge` at 0, so both arms measure bit-identical there (0 differing fields, 576 station-reads). Pinned by `tests/depth-model.test.js`; see MEASUREMENT_LESSONS 12. Ships ON — this row is the revert arm, not a feature flag | A/B revert |
| `knee` | `0`, or a fraction in [0, 1] | `0.75` | A/B revert: `0` = the pre-2026-08-22 **hard clamp** on `choppyPos`' horizontal offset, `min(|off|, 20)`. A hard bound on a displacement field is a facet generator — every vertex whose desired offset exceeds it is mapped onto the **same** sphere of radius 20 m, so relative displacement across the whole over-limit neighbourhood collapses to zero and the mesh draws a **plane with a ruler-straight silhouette**: the "white faceted slab on the crest" (live report 2026-08-22). Measured at sewers/lineup `sim=42` (27,000 samples, 45 transects, defaults): the `|off|` histogram decays monotonically through the 16–18 m bin — 1599 / 979 / 652 / 504 / 430 / **247** — then **spikes to 1135** in 18–20 m, with **892 samples (3.3%) at ≥ 19.5 m** and max exactly 20.00. A tail does not do that. Those vertices carry mean `pocket` **0.222 against 0.042** for the field at large, i.e. they sit on the breaking crest. **The driver is `Sapp`, not the lip throw, and this flag does not touch that**: `#curl=1` (computes `throwMag`, never applies it, caps S at 1.0) leaves the pile-up at **922 vs 892 — unchanged**, while `#look=full` (`Sapp` 0.42 → 0.22) more than halves it, **892 → 400**. On, only the **corner** is removed (the ceiling itself is `lamcap`'s business, below): identity below the knee, then `k + (M−k)·tanh((|off|−k)/(M−k))`. C1 at the knee, **strictly monotonic** above it — so distinct desired offsets stay distinct and the neighbourhood compresses instead of flattening — and it never reaches `M`, so the mesh backstop is strictly tighter than before. `k` = 15 m leaves **94.3% of samples bit-identical** (5.74% exceed 15 m). Ships ON: a defect fix, not a look. **MEASURED AFTER, AND IT IS PARTIAL — do not read it as more.** Three arms in one build: hard clamp **892 ≥ 19.5 m (3.30%), 2 exactly on 20.00**; soft knee **667 (2.47%), zero on the bound**; **unbounded: 1073 over 20 m, max 73.6 m**. The raw field wants to move up to 73.6 m on a ~5 m crest band, so the knee removes the corner and the coincident-vertex population but still crowds that 4% into the last 2 m — **the faceted slab is still visible from the lineup camera**. Root cause is upstream in `lam = S/(aEst·k²)`: the over-20 m samples carry mean `pocket` **0.317 vs 0.042** (7.5×) at median y 1.84 m. Re-measured on the **unbounded** arm (the first pass of the `#curl`/`#look` A/B was run on clamped output, where two different S values both exceed the bound and read identical — that comparison could not have found a difference): shipped 1073 / max 73.6 / mean 4.49 m; `#curl=1` 1036 (−3%) / 70.9 / 4.31; `#look=full` 519 (−52%) / 60.0 / 2.70. `Sapp` confirmed as the driver on unsaturated data, cusp cap confirmed irrelevant — but even `Sapp` = 0.22 leaves a 60 m tail, so halving it is not a bound either. Next fix is to bound `lam` against the local crest spacing (Tessendorf), a length the wave supplies, rather than a magic 20 m in world space downstream of it. **A value ≥ 20 removes the bound entirely and is an INSTRUMENT** for reading the raw offset distribution the clamp hides — not safe for the mesh, do not ship it | A/B revert |
| `lamcap` | `0` | on | A/B revert: `0` = the flat **20 m** world-space ceiling on `choppyPos`' horizontal offset. That constant has no wave in it and cannot be right at two sizes at once; measured against the unbounded arm it was also the wrong *kind* of bound — the field wants up to **73.6 m**, so a fixed ceiling only decides how hard to crush the overshoot, never whether it is legitimate. On, the ceiling is **`min(20, S/k)`**, the cusp condition written as a length: for `off = lam·grad` on `h = a·cos(kx)`, `dx/dx₀ = 1 − lam·a·k²·cos`, so `S := lam·a·k² = 1` is the cusp, and at the steepest point `|grad| = a·k`, giving `|off| = lam·|grad| = S/k`. **`S = 1` and `|off| = 1/k` are the same statement** — one dimensionless, one a length — and `1/k = Λ/2π` is the Gerstner cusp radius, so the classical bound arrives twice. Scale-free: it tightens on its own as the wavelength shoals (Λ 90 → 66 m raises k by 1.36× and the ceiling drops by the same factor), with no size calibration. **Why the field exceeds it at all — the actual defect:** `|off|` is `lam·|grad|` with the *true* local gradient, while `lam` was solved from `aEst`, a **floored amplitude estimate**; wherever the real slope outruns `aEst·k` (chop on a low-amplitude estimate, a foam-front FD spike, a trough with `aEst` on its floor) the two disagree and the product runs away. Bounding at `S/k` restores the identity the solve assumed — the same mechanism as the documented trough-crease pathology, which is what this is aimed at. **Not a cure for the crest slab, stated up front:** at the pocket `S` is near its 1.8 cap and `k` is the deep-ish carrier, so `S/k` lands at 19–26 m, close to the old 20 m there. The slab is the `Sapp` calibration (measured −52%, `#look` unbundling). **MEASURED — three arms in one build, sewers/lineup `sim=42`:** old hard clamp mean `|off|` 3.90 m / 2203 fold points; knee only 3.89 / 2250 (**indistinguishable — the knee alone does nothing**); knee + `S/k` **2.75 / 1280**. That third arm is where the faceted slab leaves the frame. **Six clocks** (t = 30/36/42/48/54/60): fold points 928/810/907/1106/674/938 → 500/365/531/482/359/524, **−41…−56%**, mean `|off|` −25…−30%, folded transects 91–100% → 74–96% (it still folds), and **crest height bit-identical at every clock** — 8.28 / 8.59 / 9.72 / 8.71 / 11.60 / 9.37 m — so the fold reduction is not paid for in wave height, which is the failure mode this area keeps producing (see `#drop`). **And it discriminates:** at Sharks (ξ 0.45, spilling, `plunge` = 0 so `Sover` ≈ 0 and the ceiling is tight) fold points go 523/574/509 → **83/85/75** at t = 36/42/48, an **84–85% cut**, folded transects 70% → 39–48%, crest unchanged. A spilling wave should not overturn and now mostly does not — crease noise removed where the physics says there is no lip, overturn kept where it says there is one, from one length with no per-site tuning | A/B revert |
| `amp` | `1` | off | feature flag: `1` = the carrier amplitude in the choppy solve, instead of the pre-2026-08-22 estimate in the choppy solve, `clamp(abs(h), max(0.6, 0.30·H₀·VIS), 12)`. `lam = S/(a·k²)` is derived from `h = a·cos(kx)`, where **`a` is the CARRIER's amplitude** — but it was read off `abs(h)`, the *instantaneous displacement*, which is a different quantity and **goes to zero twice a period**. Three known pathologies trace to that one substitution: the trough creases (the 2026-08-11 "pack ice" polygonal crack web — whose fix was to **floor** the estimate rather than stop using the wrong signal), the 20 m offset clamp's pile-up (`|off| = lam·|grad|` runs away wherever the true slope outruns `aEst·k` — raw offsets measured to **73.6 m** on a ~5 m crest band), and the floor itself, which existed only to paper over the first. On, `ocean()` returns the number it has always computed — `amp = 0.5·H_eff·grow·decay·env·shoreFade`, scaled by VIS — as an out-parameter. **Carrier only**: no chop, no boil, no whitewater mound, no setup lift, which is the point — those are the terms that polluted the estimate and none of them appears in the model the cusp solve is derived from. Costs nothing: `ocean()` is already evaluated five times per vertex for the FD and this is a value it holds anyway. The clamp survives as a guard rather than a mechanism — the low end drops to 0.05 m (a genuinely small carrier in a lull *should* sharpen little) but not to zero, or `lam` divides by nothing; the high end is unchanged. **DEFAULT OFF, on a measurement rather than caution.** The substitution is correct and as it stands it makes things WORSE, because `S` was calibrated on top of the wrong amplitude. Bounded, sewers t = 36/42/54: fold points 365/531/359 → **415/563/440 (+6…+23%)**, folded transects 78/96/74% → 83/96/87%. Unbounded, where the mechanism shows: **max `|off|` 73.4 → 145.4 m, doubled**, while the *bulk* improves (samples over 20 m 455 → 375, fold points 1091 → 940). The carrier amplitude fixes the middle of the distribution and blows up the tail — in the lull and the far field, where `amp → 0` and `lam = S/(a·k²)` has nothing left to divide by. The old `0.30·H₀·VIS` floor was covering exactly that, which is why it existed. **The real finding:** with the true `a`, `S` finally means what it says (the cusp parameter of `h = a·cos`), and every `S` constant in `choppyPos` — `Sapp` 0.42, `Sover`'s `0.15 + 1.30·plunge` — was tuned against an `a` that was ~2× the carrier at crests and near zero in troughs. Re-derive those against the honest `a`, **then** flip this | feature flag |
| `throwlen` | `1` | off | feature flag: `1` = the plunging lip throw expressed as a fraction of the **cusp length `S/k`** instead of face height. **Convicted by the argmax, not by argument:** at sewers `sim=42` the largest raw offset in the stage is 145.4 m, at x = 0, 7.3 m shoreward of the break line in 3.67 m of water with `pocket` 0.80; disabling throw and drop (`#curl=1` → `legacyLip = 0`) drops it to 94.0 m at the same station, so this term alone contributes **~51 m of horizontal displacement on a crest whose ceiling is 7.34 m**. The form is why: `mix(5.0, 0.72)·pocket·plunge·hM·lipJit·lipTip` is metres of *face height* times a magic 5.0 and three dimensionless factors — a magnitude with **no length of the wave's in it**, exactly the defect the offset bound had before `#lamcap`, bounded by nothing until the final ceiling catches it. `hM` is also capped at 3.5 m, so on a big day the throw stops growing with the wave and saturates — the same size-blindness SIZE_AUDIT removed from the foam terms. On, the throw is `THROW_FRAC · pocket · plunge · lipJit · lipTip · S/k`: dimensionless fraction of a length the wave supplies, so it shoals with the wavelength on its own, grows with the overturn through `S`, and cannot outrun the ceiling that bounds the whole offset because it is measured in the same units as that ceiling. **MEASURED NEUTRAL — the scaling it was built for does not materialise.** Swept H₀ 0.7→3.0 at pocket stations, unbounded, mean `|off|`: shipped 6.91 / 8.55 / 11.79 / 14.83 / 16.01 / 16.03 m against cusp-length 6.67 / 8.13 / 11.14 / 14.13 / 15.27 / 15.31 — **ratio flat at 0.945–0.965 across the whole range.** A dimensionally honest refactor and a measurable no-op; default OFF, recorded rather than dressed up. **Why it is flat is the finding:** the shipped row saturates 16.01 → 16.03 from H₀ 2.5 to 3.0 (`hM`'s 3.5 m cap, the documented size-blindness) and the new row saturates just as hard, 15.27 → 15.31, because at the pocket **`S` is pinned at its 1.8 clamp** whatever the swell is, so `S/k` is near-constant too. The throw is not size-blind because of the length it is measured in — **the size-scaling of the whole fold-and-throw system dies at `clamp(Sapp + Sover, 0, 1.8)`**, where `sizeGate` carries the breaking excess into `Sover` and the clamp then discards it exactly where the wave is biggest. A mesh guard doing duty as a physical statement, the same confusion `OFF_MAX_M` was making before `#lamcap` | feature flag |
| `sgrow` | `1` | off | feature flag: `1` = let the breaking-excess size signal past the two clamps that were eating it. **Measured at Sewers pocket stations:** mean excess `H₀Kₛ/γh` runs **0.43 / 0.95 / 1.62 / 1.94** across H₀ 0.7 / 1.5 / 2.5 / 3.0 — a clean 4.5× monotone size signal, exactly what "a bigger day breaks harder" should feed on. It was then discarded twice before reaching a pixel: `sizeGate` clamps it at **1.5** (saturating by H₀ 2.5 — measured 1.487 → 1.500 from 2.5 to 3.0) and `S` clamps the sum at **1.8**. That is why the lip throw saturates in *both* forms (shipped 16.01 → 16.03, cusp-length 15.27 → 15.31 over the same H₀ step) and why re-expressing it in the wave's own length changed nothing — the length was never the problem, the caps were. On, `sizeGate` runs to 3.0 and the `S` cap grows as `1.8 + 0.8·max(excess − 1, 0)` under a hard mesh backstop `S_CAP_HARD` = 3.2. **The card day sits at excess 0.95**, so growth keyed to `(excess − 1)` is exactly zero there and the calibration day is unchanged *by construction, not by tuning*; below the breaking limit nothing changes either, and only days that genuinely break harder than the card day get more. Letting `S` grow is safe only *because* of `#lamcap` — the offset ceiling is `S/k`, so it grows with `S` instead of being outrun by it. The hard cap survives as a guard well above the working range rather than a physical statement inside it, which is the distinction `OFF_MAX_M` failed to make before `#lamcap` | feature flag |
| `shelter` | `0` | on | A/B revert: `H_eff` sheltering field (MODEL.md §2.6.7) — smaller/weaker waves down-point, bake AND drawn field together. `0` = flat H₀ | A/B revert |
| `lipn` | `0` | on | A/B revert: `0` = the pre-2026-08-19 **size-blind pocket→whitewater path**, both limbs together (model `lipFoam`, GRID_FRAG's pocket foam floor). Every other foam term carries the SIZE_AUDIT factor — now named once as `foamSizeAt()` in `shared/model-glsl.js`, `clamp(H₀·shelter/1.5, 0.55, 1.6)`, exactly **1.0 at the 1.5 m card day** — via `sizeFoam` or via `sizeAmp` inside the lifecycle. These two did not: `lipFoam` was documented "xi-owned", which is a real claim about ξ but not a substitute for size (a plunging lip throws more foam than a spilling one **and** a 2.5 m lip throws more than a 0.6 m one), and at the break line the pocket Gaussian is 1 so even `pockS` dropped out; the fragment floor encoded a **relative** claim ("the head never renders dimmer than its own trailing bore") as the **absolute** constant `0.72`, while the bore it floors against is size-scaled down to 0.55. So as H₀ fell, every other term was attenuated and foam's composition shifted onto two terms that did not know how big the wave was. **The defect it fixes** (QA sheet `sets-locations-seasons`, Sewers, ξ 1.15 → coefficient 1.101): at `month=august` (H₀ 0.585 m) the wave was correctly small — stage-max crest 3.34 m against January's 5.17 — yet foam read 0.929 against October's 0.385 at H₀ 0.801, i.e. **more whitewater at less than half the swell**. Convicted by decomposition at the pinned set-peak clock: `sizeFoam` 0.550 (Aug) vs 0.550–0.606 (Oct), a 2% difference, so no size term explained a 2.4× foam gap; `brk`/`gate`/`mask`/`reef`/`env²` all unchanged; what moved was `crestNear > 0.5` at **3/64 → 43/64** stage stations and mean `lipFoam` **0.039 → 0.519** (13×) at an unchanged ξ coefficient. **Not a peel collapse** — the documented 2026-08-16 `month` failure class was checked first and refuted: stage α at Sewers is 6.2°/3.3°/3.5° for jan/oct/aug (flat, not collapsing) and an H₀ sweep 0.40→1.60 m in 0.05 steps found **no threshold** — α moves smoothly 12.3° → 2.9° → 9.1° and the pocket spread *oscillates* with H₀ (`pocketHiN` 4/13/20/15/6/1/0/3/4/11/15/18/18/12/7/4/1/0/1/3/3/8/12/14 of 25), so the whole-stage-in-phase state is reachable at H₀ 1.00 and 1.60 too. That underlying closeout — Sewers' stage α sits at 3–9° against a 38° target at **every** H₀, so its break line is nearly a crest line — is a separate, pre-existing defect (TODO Track 1c′) and is **not** fixed here. Because `foamSizeAt` is 1.0 at the card day the shipped card look is unchanged by construction; only sub- and super-card days move | A/B revert |
| `env` | `0` | on | A/B revert: `0` = the pre-2026-08-18 set envelope, `0.5 + 0.5·cos(…)` — 100% modulation depth floored at **exactly zero**. On, the envelope is `(1−m) + m·cos(…)` with modulation depth `m = 0.425`, so the **peak is unchanged at 1.0 by construction** and only the trough rises, to a floor of **0.15**. This is a defect fix, not a taste knob: `measure_wave_scale.mjs` over one 166.7 s beat at secondpeak x=80 measured drawn H swinging 15.7× (6.30 → 0.40 m) and the height exaggeration falling to **0.22×** — i.e. for 3 of 10 sampled clocks the render drew water *flatter than the physical sea* (drawn H/L 0.006–0.013 vs physical 0.021), while at the set peak the same instrument says there is no steepness headroom left. The floor is **derived, not picked**: a two-component beat's envelope ranges `|a₁−a₂| … (a₁+a₂)`, so the floor IS the component amplitude ratio, and two independent estimators on the repo's own 25-year SC116 spectra agree (`PP_SPECTRAL_SETS.md` §7): adjacent-band amplitude ratio at the model's own Δf → 0.075–0.163, and matching the measured lull *duty cycle* over 108,000 set-cycle windows → 0.135–0.171. Δf is **not** retuned (§4's prohibition stands). Drives `setEnv` in GLSL and both JS twins (`model-js` heights, `sound.js` voice — the audio now swells and eases instead of going silent); `setupLiftM` deliberately keeps 100% depth (setup is broken-wave momentum flux, and lull waves do not break). Cadence 1/Δf is untouched by construction | A/B revert |

**Removed:** `#swell=` (2026-08-11). It wrote `state.swellDeg`, which nothing
read — the refraction bake takes `swellDeg: state.alpha` — so the knob looked
wired and was not; a real direction knob needs the MODEL.md §2.4/§4.5 α split
first (docs/research/EXTERNAL_VALIDITY_AUDIT_2026-08-11.md, "Direction in the
code"; TODO Track 3).
